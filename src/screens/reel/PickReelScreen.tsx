import { StyleSheet } from 'react-native';
import React from 'react';
import CustomView from '../../components/global/CustomView';

export default function PickReelScreen() {
  return <CustomView />;
}

const styles = StyleSheet.create({});
